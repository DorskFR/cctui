# This project is bun-only (see AGENTS.md). These settings exist so npm reaches
# the preinstall guard and prints a readable error: without legacy-peer-deps it
# crashes in peer resolution with "edgesOut", and without package-lock=false it
# leaves a stray lockfile behind that would shadow bun.lock.
legacy-peer-deps=true
package-lock=false
