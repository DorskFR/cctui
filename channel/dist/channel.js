#!/usr/bin/env bun
// cctui-channel bundle placeholder — run 'make build/channel' to generate
console.error("[cctui-channel] This is a placeholder bundle. Run 'make build/channel' to generate the real bundle.");
process.exit(1);
