# Example reference doc (context pack)

Reference material exposed to the dispatched agent under `/opt/context/docs/`.
A real pack puts shared conventions here — linting rules, test commands, the
result-callback shape, repo-specific gotchas — and references them from
`CLAUDE.md` or the prompt files.

This file is a neutral placeholder so the fixture pack has a non-empty `docs/`.
