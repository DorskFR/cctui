---
name: example-skill
description: Neutral placeholder skill shipped in the example context pack. Demonstrates the skills/ layout; replace with your own skill when building a real pack.
---

# example-skill

A no-op example skill. The context-pack phase copies `skills/` into
`~/.claude/skills/` so the dispatched agent discovers it like any other skill.

Replace this with a real `SKILL.md` (and any supporting files) when you author
your organization's context pack.
